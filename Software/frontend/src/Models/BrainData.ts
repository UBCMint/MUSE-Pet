interface BrainData {
    _id: string,
    focusLevel: number,
    //happinessLevel: number,
    alpha: number,
    beta: number,
    //isDead: boolean,
    //isSick: boolean
}

export default BrainData